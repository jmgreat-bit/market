--- src/hooks/useChat.test.ts
+++ src/hooks/useChat.test.ts
@@ -19,7 +19,7 @@
     let mockSupabase: Record<string, jest.Mock>;
     let mockQuery: Record<string, jest.Mock>;
     let mockChannel: Record<string, jest.Mock>;
-    let channelOnCallback: (payload: any) => void;
+    let channelOnCallback: (payload: unknown) => void;

     const mockUser = { id: 'user-1' };
     const conversationId = 'conv-1';
@@ -45,7 +45,7 @@
         };

         mockChannel = {
-            on: jest.fn().mockImplementation((type: string, config: any, callback: any) => {
+            on: jest.fn().mockImplementation((type: string, config: unknown, callback: (payload: unknown) => void) => {
                 channelOnCallback = callback;
                 return mockChannel;
             }),
@@ -86,7 +86,7 @@

         // Mocks for fetchMessages
         mockQuery.order.mockReturnThis();
-        mockQuery.then.mockImplementationOnce((resolve: any) => resolve({ data: mockMsgs, error: null }));
+        mockQuery.then.mockImplementationOnce((resolve: (val: unknown) => void) => resolve({ data: mockMsgs, error: null }));

         // Provide a custom mock for eq that doesn't shadow the chained update call
         mockQuery.eq.mockImplementation(() => mockQuery);
@@ -109,7 +109,7 @@
         mockQuery.single.mockResolvedValueOnce({ data: null, error: mockError });

         mockQuery.order.mockReturnThis();
-        mockQuery.then.mockImplementationOnce((resolve: any) => resolve({ data: [], error: null }));
+        mockQuery.then.mockImplementationOnce((resolve: (val: unknown) => void) => resolve({ data: [], error: null }));

         const { result } = renderHook(() => useChat(conversationId));

@@ -134,7 +134,7 @@

         // fetch messages resolves empty
         mockQuery.order.mockReturnThis();
-        mockQuery.then.mockImplementationOnce((resolve: any) => resolve({ data: [], error: null }));
+        mockQuery.then.mockImplementationOnce((resolve: (val: unknown) => void) => resolve({ data: [], error: null }));

         mockQuery.eq.mockImplementation(() => mockQuery);
         mockQuery.neq.mockImplementation(() => mockQuery);
@@ -178,7 +178,7 @@
         mockQuery.maybeSingle.mockResolvedValueOnce({ data: null, error: null });

         mockQuery.order.mockReturnThis();
-        mockQuery.then.mockImplementationOnce((resolve: any) => resolve({ data: [], error: null }));
+        mockQuery.then.mockImplementationOnce((resolve: (val: unknown) => void) => resolve({ data: [], error: null }));

         mockQuery.eq.mockImplementation(() => mockQuery);
         mockQuery.neq.mockImplementation(() => mockQuery);
